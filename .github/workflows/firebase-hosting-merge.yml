# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebbase Hosting on merge
"on":
  push:
    branches:
      - master
env:
  VITE_APP_FIREBASE_API_KEY: ${{vars.VITE_APP_FIREBASE_API_KEY}}
  VITE_APP_FIREBASE_AUTH_DOMAIN: ${{vars.VITE_APP_FIREBASE_AUTH_DOMAIN}}
  VITE_APP_FIREBASE_DATABASEURL: ${{vars.VITE_APP_FIREBASE_DATABASEURL}}
  VITE_APP_FIREBASE_PROJECT_ID: ${{vars.VITE_APP_FIREBASE_PROJECT_ID}}
  VITE_APP_FIREBASE_STORAGE_BUCKET: ${{vars.VITE_APP_FIREBASE_STORAGE_BUCKET}}
  VITE_APP_FIREBASE_MESSAGING_SENDER_ID: ${{vars.VITE_APP_FIREBASE_MESSAGING_SENDER_ID}}
  VITE_APP_FIREBASE_APP_ID: ${{vars.VITE_APP_FIREBASE_APP_ID}}
  VITE_APP_FIREBASE_MEASUREMENT_ID: ${{vars.VITE_APP_FIREBASE_MEASUREMENT_ID}}

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20.x]
    environment:
      name: Gita1a
      url: https://github.com
    defaults:
      run:
        working-directory: ./Gita/
    steps:
      - uses: actions/checkout@v4
      - run: npm install --legacy-peer-deps && npm run build
      - name: Firebase Extended
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_GITA1A }}"
          channelId: live
          projectId: gita1a
          entryPoint: ./Gita/

